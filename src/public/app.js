const state={worldId:'local-dev',systemEntityId:null,humanEntityId:null,agentEntityId:null}; const $=(id)=>document.getElementById(id); const id=()=>crypto.randomUUID();
function log(value){$('log').textContent=typeof value==='string'?value:JSON.stringify(value,null,2)}
async function request(path,{method='GET',actor,body}={}){const headers={};if(body)headers['content-type']='application/json';if(actor)headers['x-nh-actor-id']=actor;if(method!=='GET')headers['idempotency-key']=id();const res=await fetch(path,{method,headers,body:body?JSON.stringify(body):undefined});const data=await res.json();if(!res.ok)throw Object.assign(new Error(data.message||data.error),{data});return data}
async function wallet(entityId){return request(`/api/v1/worlds/${encodeURIComponent(state.worldId)}/wallets/${entityId}`)} async function run(fn){try{const out=await fn();log(out)}catch(e){log(e.data||{error:e.message})}}
request('/api/v1/health').then(v=>$('health').textContent=`API ${v.schemaVersion} · online`).catch(()=>$('health').textContent='API offline');
$('bootstrap').onclick=()=>run(async()=>{const v=await request('/api/v1/dev/bootstrap',{method:'POST',body:{worldId:state.worldId}});Object.assign(state,v);$('identity').textContent=JSON.stringify(v,null,2);$('fund').disabled=false;$('createAgent').disabled=false;return v});
$('fund').onclick=()=>run(async()=>{const v=await request('/api/v1/economy/mint',{method:'POST',actor:state.systemEntityId,body:{worldId:state.worldId,targetEntityId:state.humanEntityId,amountMicroE:'200000000',basisKey:`local-owner-${id()}`}});$('ownerWallet').textContent=JSON.stringify(await wallet(state.humanEntityId),null,2);return v});
$('createAgent').onclick=()=>run(async()=>{const displayId=`agent-${Date.now()}`;const v=await request('/api/v1/entities',{method:'POST',actor:state.systemEntityId,body:{worldId:state.worldId,entityType:'AGENT',displayId,name:'P0/P1 Agent'}});state.agentEntityId=v.result.entity.entity_id;$('agent').textContent=JSON.stringify(v.result.entity,null,2);$('transfer').disabled=false;return v});
$('transfer').onclick=()=>run(async()=>{const v=await request('/api/v1/economy/transfer',{method:'POST',actor:state.humanEntityId,body:{worldId:state.worldId,fromEntityId:state.humanEntityId,toEntityId:state.agentEntityId,amountMicroE:'100000000',businessKey:`owner-grant-${id()}`}});$('ownerWallet').textContent=JSON.stringify(await wallet(state.humanEntityId),null,2);$('agentWallet').textContent=JSON.stringify(await wallet(state.agentEntityId),null,2);$('activate').disabled=false;return v});
$('activate').onclick=()=>run(async()=>{const v=await request('/api/v1/economy/first-activation',{method:'POST',actor:state.agentEntityId,body:{worldId:state.worldId,entityId:state.agentEntityId}});$('activation').textContent=JSON.stringify(v.result,null,2);$('agentWallet').textContent=JSON.stringify(await wallet(state.agentEntityId),null,2);return v});
