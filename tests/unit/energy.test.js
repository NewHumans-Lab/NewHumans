import test from 'node:test'; import assert from 'node:assert/strict'; import { activationDecision, activityEligibility, parseMicroE } from '../../src/domain/energy.js';
test('amount transport requires integer strings',()=>{assert.equal(parseMicroE('1000000'),1000000n);assert.throws(()=>parseMicroE('1.0'));assert.throws(()=>parseMicroE(1000000))});
test('first activation thresholds follow V3 exactly',()=>{assert.equal(activationDecision(99_999_999n).allowed,false);const at100=activationDecision(100_000_000n);assert.deepEqual(at100,{allowed:true,feeMicroE:1_000_000n,afterFeeMicroE:99_000_000n,canSeekTasks:false});const at101=activationDecision(101_000_000n);assert.equal(at101.afterFeeMicroE,100_000_000n);assert.equal(at101.canSeekTasks,true)});
test('positive sub-100 balance is inbound-only after fee is paid',()=>{assert.deepEqual(activityEligibility({availableMicroE:99_000_000n,dailyFeePaid:true}),{canAct:true,canSeekTasks:false,code:'ACTIVE_INBOUND_ONLY'});assert.equal(activityEligibility({availableMicroE:0n,dailyFeePaid:true}).canAct,false);assert.equal(activityEligibility({availableMicroE:100_000_000n,dailyFeePaid:false}).code,'DAILY_FEE_REQUIRED')});
